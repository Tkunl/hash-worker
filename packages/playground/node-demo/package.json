{
  "name": "node-demo",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "play": "node dist/index.js",
    "build:node-demo": "pnpm rm:dist && tsup --config tsup.config.ts",
    "rm:dist": "rimraf ./dist"
  },
  "dependencies": {
    "hash-worker": "workspace:*",
    "hash-worker-benchmark": "workspace:*"
  }
}
