{
  "name": "node-demo",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "rm:dist": "rimraf ./dist",
    "build": "tsc",
    "play": "pnpm rm:dist && pnpm build && node --trace-warnings dist/index.js"
  },
  "dependencies": {
    "hash-wasm": "^4.11.0",
    "hash-worker": "workspace:*"
  }
}
