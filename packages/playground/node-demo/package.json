{
  "name": "node-demo",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "rm:dist": "rimraf ./dist",
    "build": "pnpm rm:dist && tsup --config tsup.config.ts",
    "play": "pnpm rm:dist && pnpm build && node --trace-warnings dist/index.js",
    "benchmark": "pnpm rm:dist && pnpm build && node dist/benchmark.js"
  },
  "dependencies": {
    "hash-worker": "workspace:*"
  }
}
