{
  "name": "benchmark",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "play": "node dist/index.js",
    "build:benchmark-demo": "pnpm rm:dist && tsup --config tsup.config.ts",
    "rm:dist": "rimraf ./dist"
  },
  "dependencies": {
    "hash-worker": "workspace:*",
    "hash-worker-benchmark": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}
